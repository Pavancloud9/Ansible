- name: Install and run nginx
  hosts: 172.31.41.91
  become: yes
  tasks:
  - name: Install nginx
    ansible.builtin.package:
      name: nginx
      state: present
  - name: run nginx service
    ansible.builtin.service:
      name: nginx
      state: started
