- name: Install packages
  hosts: node
  become: yes
  tasks:
  - name: Install packages
    ansible.builtin.package:
      name: {{item}}
      state: installed
    loop:
    - gcc
    - zip
    - mysql